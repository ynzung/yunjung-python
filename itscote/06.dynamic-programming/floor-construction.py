n = int(input())

d = [0] * 1001 

d[1] = 1  # 1개로 만들 수 있는 경우
d[2] = 3  # 2개로 만들 수 있는 경우
for i in range(3, n + 1):
    d[i] = (d[i - 1] + d[i - 2]) % 796796  # 이전 두 경우를 이용하여 현재 경우 계산

print(d[n])  # n개로 만들 수 있는 경우의 수 출력